<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
   <%= link_to task_path(task)  do  %>
    <li><%= task.title %> - <%= task.details %></li>
   <% end %>
  <% end %>
</ul>
<%= link_to 'Add a new task', tasks_new_path %>
